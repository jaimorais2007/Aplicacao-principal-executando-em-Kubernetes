apiVersion: v1
kind: Service
metadata:
  name: oficina-mecanica-api-svc
spec:
  selector:
    app: oficina-mecanica-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
